import React from "react";

const Register1 = ({ changeRole }) => {
  return (
    <div>
      Register1
      <button onClick={() => changeRole()}>Register</button>
    </div>
  );
};

export default Register1;
